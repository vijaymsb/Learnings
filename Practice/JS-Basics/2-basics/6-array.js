// Arrays are indicated in square brackets []
// Arrays are always objects

let colors = ['red', 'blue', 'green'];
colors[3] = 'yellow';
console.log(colors);
console.log(colors. length);
console.log(colors[2]);
