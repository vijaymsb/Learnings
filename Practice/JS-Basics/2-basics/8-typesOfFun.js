// Functions are a set of statements to perform a task or to calculate a value

// Performing a task
function fun(name, lastName) {
    console.log('Hello ' + name + '' + lastName);
}
fun('vijay ', 'kumar');

// Calculating a value
function square(number) {
    return number * number;
}
console.log(square(2));